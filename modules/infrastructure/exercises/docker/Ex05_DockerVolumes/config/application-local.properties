# This file is mounted into the container via bind mount (see docker-compose.yml).
# Changes here apply WITHOUT rebuilding the image - that is the key point of bind mount.
#
# Spring Boot picks up files from /app/config/ automatically when:
# spring.config.additional-location=/app/config/

logging.level.by.pavel=DEBUG
APP_ENV=local-dev

# Uncomment if spring-boot-starter-data-jpa is added to pom.xml:
# spring.datasource.url=jdbc:postgresql://postgres:5432/localdb
# spring.datasource.username=dev
# spring.datasource.password=devpassword
